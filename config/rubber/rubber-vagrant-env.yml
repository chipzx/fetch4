cloud_provider: vagrant
 
cloud_providers:
  vagrant:
    # Allow rubber to connect directly to instance in addition to use through vagrant commands.
    key_name:  ubuntu@ubuntu
    key_file: "#{File.expand_path('~/.ssh/authorized_keys')}"
